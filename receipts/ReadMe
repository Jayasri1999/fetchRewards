Use below commands to start the application in docker:

1. docker build -t receipts .
2 docker run -p 8080:8080 receipts

Use the port 8080 
Ex: if running in local use: http://localhost:8080/receipts/process
